# Auto-Prompt 离线安装自测清单

安装包：

- `D:\projects\auto-prompt-desktop\src-tauri\target\release\bundle\nsis\auto-prompt-app_0.1.0_x64-setup.exe`

测试环境要求：

- Windows 10/11 x64
- 测试机无外网
- 测试机未预装 Python 更佳
- 测试机未预装 WebView2 更佳

## 1. 安装验证

- 双击安装包，确认安装流程可正常完成。
- 安装过程中不应提示联网下载依赖。
- 安装完成后桌面或开始菜单可看到 `Auto-Prompt`。
- 首次启动应用，窗口可正常打开，不白屏、不闪退。

通过标准：

- 安装成功
- 应用能启动
- 无“缺少运行时”“需要联网下载组件”之类提示

## 2. 环境检查页验证

- 打开应用里的环境检查页面。
- 确认 Python 显示为可用。
- 优先确认版本信息包含“内置”字样。
- 确认 `openai`、`openpyxl`、`pymupdf` 显示为已安装。

通过标准：

- Python 可用
- 三个 Python 包均为已安装
- 不需要再点“安装 Python”或“安装依赖”

## 3. Skills 资源验证

- 打开应用内和 `skills` 相关的页面或功能入口。
- 确认能列出内置技能，不出现“未找到 skills 目录”。
- 至少验证以下能力入口可正常进入：
- `doc-extract-prompt-gen`
- `material-classifier`
- `factor-json-generator`
- `review-rule-generator`

通过标准：

- 内置 skills 可见
- 不报资源缺失错误

## 4. 提示词生成验证

测试数据建议使用项目内现成样例目录之一：

- `D:\projects\auto-prompt-desktop\材料集`
- `D:\projects\auto-prompt-desktop\分类材料集`
- `D:\projects\auto-prompt-desktop\生育津贴支付-材料集`

验证步骤：

- 打开“提示词生成”功能。
- 选择一个带 `factors.xlsx` 和图片/PDF 的目录。
- 运行读取要素。
- 运行提示词生成。

通过标准：

- 能读取 `factors.xlsx`
- 能执行 Python 脚本
- 能生成输出文件
- 不报“找不到 python3 / openpyxl / pymupdf / skills”之类错误

说明：

- 如果这一步需要调用模型接口，离线机上应预期因无网络失败。
- 但失败应表现为 API/网络错误，而不是本地环境缺失错误。

## 5. PDF 处理验证

- 选择包含 PDF 的样例材料目录。
- 执行需要读取或验证 PDF 的功能。

通过标准：

- PDF 能被正常处理
- 不报缺少 `pymupdf` 或 PDF 转图失败的本地依赖错误

## 6. 材料分类验证

- 打开“材料分类”页面。
- 选择 `D:\projects\auto-prompt-desktop\分类材料集` 或类似目录。
- 点击读取待分类文件。
- 尝试启动分类。

通过标准：

- 能识别待分类文件
- 能正确找到分类模板和 skills 资源
- 若无网络，失败应是模型接口不可用，不应是本地 Python/脚本缺失

## 7. 要素 JSON 生成功能验证

- 打开“要素 JSON”页面。
- 选择带材料子目录和 `factors.xlsx` 的样例目录。
- 执行生成。

通过标准：

- 能生成 JSON 文件
- 不报 Python 环境缺失

## 8. 审查规则生成功能验证

- 打开“审查规则”页面。
- 选择带 `factors.xlsx` 的样例目录。
- 尝试执行生成。

通过标准：

- 本地脚本可启动
- 若无网络，报错应集中在模型调用，而不是缺少本地依赖

## 9. 案例库读写验证

- 打开案例库页面。
- 尝试导入一个案例文件或执行会写入案例库的操作。
- 关闭应用后重新打开，确认数据仍在。

通过标准：

- 案例库可写
- 重启后数据仍存在

说明：

- 这一项用于验证应用已把可写数据放到用户目录，而不是安装目录。

## 10. 回归观察点

- 全程不应出现：
- `python3` 找不到
- `No module named openai/openpyxl/fitz`
- `未找到 skills 目录`
- `权限不足，无法写入安装目录`
- 启动后白屏或闪退

## 11. 建议记录项

- 测试机器系统版本
- 是否预装 Python
- 是否预装 WebView2
- 安装是否成功
- 首启是否成功
- 环境检查页结果
- 各功能页面通过/失败情况
- 失败时截图和报错原文

